include ../../../components/FunctionDocumentation/FunctionDocumentation
include ../../../components/NotebookLink/NotebookLink

.Display__title
  .Display__titleFunc def
  | table
.Display__breadcrumb cauldron.display.table()

+createFunctionDoc(display_functions.table)(hideTitle=true)

.Display__h1 Basic Usage

:highlight()
  import cauldron as cd
  import numpy as np
  import pandas as pd

  # Create a data frame of ten rows by four columns populated with random
  # integers
  df = pd.DataFrame(np.random.randint(0,10,size=(10, 4)), columns=list('abcd'))

  # Show that dataframe as a table in the notebook
  cd.display.table(df)

.Display__h1 Examples

+createNotebookLink('v1', 'bokeh-basics', 'Bokeh Basics')
  div.
    Demonstrates plotting with the bokeh library in Cauldron. Uses the table
    function early in the notebook to display the source data set for the
    notebook plots.

+createNotebookLink('v1', 'plotly-basics', 'Plotly Basics')
  div.
    Demonstrates plotting with the plotly library in Cauldron. Uses the table
    function early in the notebook to display the source data set for the
    notebook plots.
